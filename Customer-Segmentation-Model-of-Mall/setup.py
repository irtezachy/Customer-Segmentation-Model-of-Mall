from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Build an unsupervised clustering model to segment customers into correct groups.',
    author='Irteza',
    license='',
)
